# Copy this file to .streamlit/secrets.toml and paste your key between the quotes.
# secrets.toml is in .gitignore, so your key is never committed.
# Get a free key at https://console.groq.com/keys
GROQ_API_KEY = ""
